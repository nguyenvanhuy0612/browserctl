node_modules/
npm-debug.log*
*.tgz

# Local runtime artefacts
.env
*.log
tests/**/artifacts/

# Written by the bridge at runtime, never source:
#   calls.jsonl    — opt-in per-call log (BROWSERCTL_CALL_LOG)
#   telemetry.jsonl — benchmark harness output
bridge/calls.jsonl*
bridge/telemetry.jsonl*
test/benchmark/runs/

# OS / IDE
.DS_Store
.idea/
.vscode/

# third-party improvement plans: internal input, kept out of the public repo
improvements/

# session case study: quotes a real person's contact details from the mail it read
docs/history/case-study-gmail-*.md
